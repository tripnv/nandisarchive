repos:
-   repo: local
    hooks:
    -   id: format-html
        name: Format HTML (djLint)
        entry: uv run --with djlint djlint templates/ --reformat --quiet
        language: system
        pass_filenames: false
        always_run: true